version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./documentos:/app/documentos:ro
      - ./uploads:/app/uploads
      - ./output:/app/output
    environment:
      - FLASK_DEBUG=0
    restart: unless-stopped
